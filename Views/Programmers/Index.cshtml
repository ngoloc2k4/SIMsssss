@model IEnumerable<SIM_Project.Models.Programmer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProgramName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProgramName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProgrammerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProgrammerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProgrammerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
